# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Fuzzy string matching
fuzzywuzzy>=0.18.0
python-levenshtein>=0.27.0

# Web scraping
beautifulsoup4>=4.14.0
requests>=2.32.0
lxml>=6.0.0

# Optional: for progress bars (if we add them later)
# tqdm>=4.65.0
